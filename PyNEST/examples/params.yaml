# This file is autogenerated by generate_example_config().
#
# Uncomment and edit only the parameters you want to change.
# Every parameter that stays commented out takes its default value
# from the Parameters class (parameter_definitions.py).
# The values shown here are those defaults.
#
# To modify defaults or metadata (descriptions, units, LaTeX symbols),
# edit the parameter definitions in parameter_definitions.py
# and run generate_example_config() again.

N_scaling: 0.2
# Scaling factor determining network size.
# unit: 
# LaTeX: $\alpha_N$
# section: network

K_scaling: 0.2
# Scaling factor determining synapse numbers.
# unit: 
# LaTeX: $\alpha_K$
# section: network

# full_num_neurons: [20683, 5834, 21915, 5479, 4850, 1065, 14395, 2948]
# Default number of neurons in each population $x$ of the full-scale network.
# unit: 
# LaTeX: $\tilde{N}_x$
# section: network

# conn_probs: [[0.1009, 0.1689, 0.0437, 0.0818, 0.0323, 0.0, 0.0076, 0.0], [0.1346, 0.1371, 0.0316, 0.0515, 0.0755, 0.0, 0.0042, 0.0], [0.0077, 0.0059, 0.0497, 0.135, 0.0067, 0.0003, 0.0453, 0.0], [0.0691, 0.0029, 0.0794, 0.1597, 0.0033, 0.0, 0.1057, 0.0], [0.1004, 0.0622, 0.0505, 0.0057, 0.0831, 0.3726, 0.0204, 0.0], [0.0548, 0.0269, 0.0257, 0.0022, 0.06, 0.3158, 0.0086, 0.0], [0.0156, 0.0066, 0.0211, 0.0166, 0.0572, 0.0197, 0.0396, 0.2252], [0.0364, 0.001, 0.0034, 0.0005, 0.0277, 0.008, 0.0658, 0.1443]]
# Connection probabilities for each pair of pre- and postsynaptic populations $x$ and $y$ (first index: target; second index: source)
# unit: 
# LaTeX: $C_{yx}$
# section: network

# full_mean_rates: [0.903, 2.965, 4.414, 5.876, 7.569, 8.633, 1.105, 7.829]
# Mean firing rates of the different populations $x$ in the non-scaled version of the microcircuit (same order as in `populations`; required for network scaling).
# unit: spikes/s
# LaTeX: $\tilde{\nu}_x$
# section: network

# CC_type: dc
# Type of cortico-cortical input ('poisson' or 'dc').
# unit: 
# LaTeX: bg\_input\_type
# section: network

# K_CC_full: [1600, 1500, 2100, 1900, 2000, 1900, 2900, 2100]
# Number of cortico-cortical inputs per neuron (in-degree) in the full-scalenework for each cortical population $y$ (same order as in `populations`).
# unit: 
# LaTeX: $\tilde{K}_{\mathcal{C}_y}$
# section: network

# rate_CC: 8.0
# Rate of cortico-cortical inputs.
# unit: spikes/s
# LaTeX: $\nu_\text{CC}$
# section: network

# delay_CC: 1.5
# Spike transmission delay of cortico-cortical inputs.
# unit: ms
# LaTeX: $\bar{d}_\text{CC}$
# section: network

# neuron_model: iaf_psc_exp
# NEST neuron model name.
# unit: 
# LaTeX: neuron\_model
# section: neuron

# V_rest: -65.0
# Resting potential.
# unit: mV
# LaTeX: V_\text{rest}
# section: neuron

# V_th: -50.0
# Spike threshold potential.
# unit: mV
# LaTeX: $\theta$
# section: neuron

# V_reset: -65.0
# After-spike reset potential.
# unit: mV
# LaTeX: $V_\text{reset}$
# section: neuron

# C_m: 250.0
# Membrane capacitance.
# unit: pF
# LaTeX: $C_\text{m}$
# section: neuron

# tau_m: 10.0
# Membrane time constant.
# unit: ms
# LaTeX: $\tau_\text{m}$
# section: neuron

# tau_ref: 2.0
# Duration of absolute refractory period.
# unit: ms
# LaTeX: $\tau_\text{ref}$
# section: neuron

# weight_exc_mean: 0.15
# Mean weight of excitatory synapses (PSP amplitude).
# unit: mV
# LaTeX: $J$
# section: synapse

# weight_cv: 0.1
# Coefficient of variation of synaptic weight distributions (ratio between standard deviation and mean).
# unit: 
# LaTeX: $\text{CV}_\text{w}$
# section: synapse

# g: -4.0
# Relative weight of inhibitory synapses (ratio of inhibitory and excitatory synaptic weights).
# unit: 
# LaTeX: $g$
# section: synapse

# tau_syn: 0.5
# Time constant of postsynaptic currents.
# unit: ms
# LaTeX: $\tau_\text{s}$
# section: synapse

# delay_exc_mean: 1.5
# Mean spike transmission delay of excitatory connections.
# unit: ms
# LaTeX: $\bar{d}_\text{E}$
# section: synapse

# delay_inh_mean: 0.75
# Mean spike transmission delay of inhibitory connections.
# unit: ms
# LaTeX: $\bar{d}_\text{I}$
# section: synapse

# delay_cv: 0.5
# Coefficient of variation of delay distributions (ratio between standard deviation and mean).
# unit: 
# LaTeX: $\text{CV}_\text{d}$
# section: synapse

# V0_type: optimized
# Type of initial condition for the membrane potentials ('original' or 'optimized'). 'original': uniform mean and standard deviation for all populations. 'optimized': population-specific mean and standard deviation (default).
# unit: 
# LaTeX: V0\_type
# section: initialization

# V0_mean_original: -58.0
# Mean of initial membrane potentials in case V0_type = 'original'.
# unit: mV
# LaTeX: $V0\_mean\_original$
# section: initialization

# V0_std_original: 10.0
# Standard deviation of initial membrane potentials in case V0_type = 'original'.
# unit: mV
# LaTeX: $V0\_std\_original$
# section: initialization

# V0_mean_optimized: [-68.28, -63.16, -63.33, -63.45, -63.11, -61.66, -66.72, -61.43]
# Population-specific mean of initial membrane potentials in case V0_type = 'optimized' (same order as in `populations`).
# unit: mV
# LaTeX: $V0\_mean\_optimized$
# section: initialization

# V0_std_optimized: [5.36, 4.57, 4.74, 4.94, 4.94, 4.55, 5.46, 4.48]
# Population-specific standard deviation of initial membrane potentials in case V0_type = 'optimized' (same order as in `populations`).
# unit: mV
# LaTeX: $V0\_std\_optimized$
# section: initialization

# thalamic_input: False
# Turn (transient) thalamic input on ('True) or off ('False'; default).
# unit: 
# LaTeX: thalamic\_input
# section: stimulus

# th_start: 700.0
# Onset time of thalamic input.
# unit: ms
# LaTeX: $t_\text{start}$
# section: stimulus

# th_duration: 10.0
# Duration of thalamo-cortical input.
# unit: ms
# LaTeX: $\Delta_\text{TC}$
# section: stimulus

# th_rate: 120.0
# Rate of thalamo-cortical input.
# unit: spikes/s
# LaTeX: $\nu_\text{TC}$
# section: stimulus

# num_th_neurons: 902
# Number of thalamic neurons.
# unit: 
# LaTeX: $N_\text{TC}$
# section: stimulus

# conn_probs_th: [0.0, 0.0, 0.0983, 0.0619, 0.0, 0.0, 0.0512, 0.0196]
# Probabilities of connections from the thalamus to each cortical populations $y$ (same order as in `populations`).
# unit: 
# LaTeX: $C_{y,\mathcal{T}}$
# section: stimulus

# dc_transient: False
# Turn (transient) DC input on ('True) or off ('False'; default).
# unit: 
# LaTeX: dc\_transient
# section: stimulus

# dc_transient_start: 650.0
# Onset time of transient DC input.
# unit: ms
# LaTeX: $t_\text{start}^\text{DC}$
# section: stimulus

# dc_transient_dur: 100.0
# Duration of transient DC input.
# unit: ms
# LaTeX: $\Delta_\text{DC}$
# section: stimulus

# dc_transient_amp: 0.3
# Amplitude of transient DC input (final amplitude is population-specific and will be obtained by multiplication with 'K_CC_full').
# unit: pA
# LaTeX: $I_\text{DC}$
# section: stimulus

# t_presim: 500.0
# Duration of presimulation (warmup).
# unit: ms
# LaTeX: t\_presim$
# section: simulation

# t_sim: 1000.0
# Duration of (main) simulation.
# unit: ms
# LaTeX: t\_sim$
# section: simulation

# sim_resolution: 0.1
# Simulation time resolution.
# unit: ms
# LaTeX: sim\_resolution$
# section: simulation

# rec_dev: ['spike_recorder']
# List of recording devices ('spike_recorder' [default] and/or 'voltmeter'). Nothing will be recorded if an empty list is given.
# unit: 
# LaTeX: rec\_dev
# section: simulation

data_path: data_scale_0.20
# Path for storage of simulation data and metadata. Relative paths are resolved against the current working directory.
# unit: 
# LaTeX: data\_path
# section: simulation

# rng_seed: 55
# Seed for NEST random number generator (used for connectivity, initial conditions, and Poissonian spike input).
# unit: 
# LaTeX: rng\_seed
# section: simulation

# local_num_threads: 4
# Number of threads per MPI process. Note: when up-scaling the network, the model may not run correctly if there are < 4 virtual processes (i.e, a thread in an MPI process). If there are 4 or more MPI processes, this value can be set to 1.
# unit: 
# LaTeX: local\_num\_threads
# section: simulation

# rec_V_int: 1.0
# Time resolution of membrane potential recordings.
# unit: ms
# LaTeX: rec\_V\_int
# section: simulation

# overwrite_files: True
# If 'True' (default), existing files will be overwritten. If 'False', a NESTError is raised if the files already exist.
# unit: 
# LaTeX: overwrite\_files
# section: simulation

# print_time: True
# If 'True' (default), the simulation progress is printed. This should only be used if the simulation is run on a local machine.
# unit: 
# LaTeX: print\_time
# section: simulation

# store_metadata: True
# If 'True' (default), metadata (parameter values, node IDs, and software requirements) will be stored together with the simulation data. 
# unit: 
# LaTeX: store\_metadata
# section: simulation

